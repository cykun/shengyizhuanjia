import { PhoneConfirmDirective } from './phone-confirm.directive';

describe('PhoneConfirmDirective', () => {
  it('should create an instance', () => {
    const directive = new PhoneConfirmDirective();
    expect(directive).toBeTruthy();
  });
});
